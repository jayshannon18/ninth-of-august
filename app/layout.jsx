export const metadata = {
  title: '9th of August',
  description: 'Premium photography & videography booking platform. AI-matched to your exact project.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
